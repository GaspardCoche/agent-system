name: Test Reusable 2
on:
  workflow_dispatch:

jobs:
  call-simple:
    uses: ./.github/workflows/_test-reusable-simple.yml
    with:
      agent_role: tester
      max_turns: "2"
    secrets: inherit
