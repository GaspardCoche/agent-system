name: Test Reusable 4
on:
  workflow_dispatch:

jobs:
  call-claude-min:
    uses: ./.github/workflows/_test-reusable-claude-min.yml
    with:
      agent_role: tester
      task_json: '{"task_id":"test"}'
      max_turns: "2"
    secrets: inherit
