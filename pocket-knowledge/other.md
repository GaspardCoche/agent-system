# Expertise other — appris par Pocket
- (2026-07-06) Label GitHub 'cat:content' n'existait pas dans le repo — créé (couleur 0e8a16) avant de l'appliquer aux tâches de rédaction.
- (2026-07-07) Gaspard veut une notif push quand une tâche longue (ex: expansion du vault) se termine — l'infra existe déjà (pocket.yml step 'Notify (push)' + scripts/pocket_push.py, VAPID) et se déclenche auto en fin de tâche pocket via:phone. Vérifier que l'appareil 'ordinateur' est bien abonné (pocket-data/sub-*.json) via la PWA.
- (2026-07-07) Notif fin de tâche CLI locale (hors Pocket) : hors de portée du cloud, ça se règle avec un hook Stop local (~/.claude/settings.json), à poser via /update-config en session Claude Code locale — pas via Pocket.
- (2026-07-07) J'ai un accès lecture (VAULT_DEPLOY_KEY, non shallow) au dépôt git claude-knowledge-vault — donc je PEUX voir un état d'avancée indirect (dernier commit, fichiers touchés) d'une session locale qui pousse au fil de l'eau, même sans notif temps réel. Toujours checker git log avant de dire 'aucune visibilité'.
- (2026-07-07) Interface Pocket = PWA dans docs/pocket/ (app.js/index.html/style.css/sw.js). Dictée vocale: fonction wireMic(micId,fieldId,msgId,onInput) dans app.js câble un bouton sur un champ (webkitSpeechRecognition, fr-FR). Bump APP_VERSION + CACHE sw.js à chaque changement pour forcer la MAJ PWA. Après dictée programmatique, relancer manuellement l'auto-resize du textarea (l'event input ne part pas).
